name: MOMI Wiki Scraper

on:
  workflow_dispatch: # 允许手动点击按钮触发
  schedule:
    - cron: '0 2 * * 1' # 每周一凌晨 2 点自动运行 (可选)

jobs:
  scrape-and-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🐹 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: 🌐 Install Chrome
        # Ubuntu Latest 自带 Chrome，但为了保险起见，我们显式安装最新版
        run: |
          wget -q -O - https://dl-google.com/linux/linux_signing_key.pub | sudo apt-key add -
          sudo sh -c 'echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: 🏃 Run Crawler
        run: |
          # 初始化模块 (如果仓库里没有 go.mod)
          if [ ! -f go.mod ]; then
            go mod init gitbook-scraper
            go get -u github.com/chromedp/chromedp
            go get -u github.com/PuerkitoBio/goquery
            go get -u github.com/JohannesKaufmann/html-to-markdown
          else
            go mod tidy
          fi
          
          # 运行爬虫
          echo "🚀 Starting Scraper..."
          go run main.go

      - name: 📦 Zip Knowledge Base
        run: |
          # 检查是否有文件生成
          if [ -d "knowledge_base" ]; then
            zip -r momi_knowledge_base.zip knowledge_base
          else
            echo "❌ No knowledge base directory found!"
            exit 1
          fi

      - name: 📤 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: momi-wiki-markdown
          path: momi_knowledge_base.zip
          retention-days: 90
